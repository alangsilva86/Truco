# Optional: local proxy target for the Vite dev server.
TRUCO_PROXY_TARGET=http://127.0.0.1:2567

# Optional: comma-separated list of allowed HTTP origins for the server.
# Supports wildcards, e.g. https://*.vercel.app
TRUCO_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://*.vercel.app

# Optional: shared Redis for Colyseus room cache/presence in production.
# Required if you run multiple instances or need room lookup to survive across processes.
REDIS_URI=

# Optional: server-side reconnect window in seconds.
# Defaults to 60 seconds and should stay aligned with the client recovery budget.
RECONNECT_WINDOW_SECONDS=60

# Optional: override HTTP room lookup/version/health origin.
# Leave empty in local development to use the Vite proxy.
VITE_SERVER_HTTP_URL=

# Optional: override the Colyseus websocket origin.
# Leave empty in local development to use ws://<current-host>:2567.
VITE_SERVER_WS_URL=

# Optional: total client-side recovery budget in milliseconds.
# Vite only exposes env vars prefixed with VITE_ to the browser bundle.
VITE_CLIENT_RECONNECT_BUDGET_MS=55000
