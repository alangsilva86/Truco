services:
  - type: web
    runtime: node
    name: truco-server
    plan: starter
    region: oregon
    buildCommand: npm ci && npm run build:server
    startCommand: npm run start:server
    healthCheckPath: /health
    envVars:
      - key: REDIS_URI
        sync: false
      - key: RECONNECT_WINDOW_SECONDS
        value: 60
    buildFilter:
      paths:
        - apps/server/**
        - packages/**
        - package.json
        - package-lock.json
        - tsconfig.base.json
        - .node-version
        - render.yaml
